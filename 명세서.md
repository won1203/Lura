# 프로젝트 개발 명세서 (Project Specification)

## 1. 프로젝트 개요
* **프로젝트명:** Lura
* **프로젝트 목표:** 오디오 데이터셋 기반의 소리 스트리밍 및 재생 안드로이드 앱 개발
* **핵심 기능:** 압축 및 필터링된 음원 데이터를 사용자에게 제공하고, 편리한 UI/UX를 통한 오디오 재생 경험 제공

## 2. 기술 스택 및 인프라 (Tech Stack)
* **프론트엔드 (App):** Android Studio
* **백엔드 (Server):** Java Spring Boot
* **데이터베이스:** MySQL
* **스토리지 및 클라우드:** AWS S3 (음원 데이터셋 보관), AWS Lambda (필요 시 서버리스 아키텍처 연동)

## 3. 주요 화면 및 기능 (UI First 설계)
### 3.1. 메인 화면 (Home)
* 카테고리별 소리(음원) 목록 제공
* 최신 업데이트 또는 추천 소리 리스트 노출

### 3.2. 재생 화면 (Player)
* 기본 재생 컨트롤 (재생, 일시정지, 이전/다음)
* 재생 중인 음원 메타데이터 (제목, 태그, 총 길이 등) 표시
* 백그라운드 재생 지원 및 상태바 컨트롤

### 3.3. 부가 기능 화면 (Optional)
* **타이머 기능:** 지정된 시간 후 앱 및 재생 종료
* **보관함:** 즐겨찾기(좋아요)한 소리 목록 저장 및 조회

## 4. API 명세서 초안 (Mock 데이터 활용 후 연동)
| 기능 명칭 | HTTP Method | Endpoint | 설명 및 비고 |
| :--- | :--- | :--- | :--- |
| 전체 음원 조회 | GET | `/api/v1/sounds` | 전체 소리 목록 및 메타데이터 반환 |
| 카테고리별 조회 | GET | `/api/v1/sounds/category/{id}` | 특정 카테고리에 속한 소리 목록 반환 |
| 음원 스트리밍 | GET | `/api/v1/sounds/{id}/play` | AWS S3 파일 접근을 위한 URL (또는 Presigned URL) 발급 |
| 즐겨찾기 추가 | POST | `/api/v1/favorites/{sound_id}` | 특정 음원을 사용자의 보관함에 추가 |

## 5. 데이터베이스 스키마 초안 (MySQL)
* **`Sound` 테이블:** 소리 메타데이터 관리
    * `id` (PK), `title`, `category`, `s3_file_url`, `duration`, `created_at`
* **`User` 테이블 (필요 시):** 사용자 정보 관리
    * `id` (PK), `email`, `created_at`
* **`Favorite` 테이블:** 즐겨찾기 데이터 매핑
    * `id` (PK), `user_id` (FK), `sound_id` (FK)

## 6. 개발 마일스톤 (Milestone)
- [ ] **Phase 1: UI 뼈대 구축**
    - 안드로이드 스튜디오 프로젝트 세팅 및 주요 화면 레이아웃(XML/Compose) 작성
    - Mock(가짜) 데이터를 활용한 화면 전환 및 동작 테스트
- [ ] **Phase 2: 데이터 연동 준비**
    - 필터링된 `.zip` 데이터셋 해제 및 AWS S3 업로드
    - S3 접근 권한 및 구조 세팅
- [ ] **Phase 3: 백엔드 로직 개발**
    - Spring Boot 프로젝트 세팅 및 MySQL DB 연동
    - API 엔드포인트 개발 및 S3 통신 로직 구현
- [ ] **Phase 4: 프론트-백엔드 통합**
    - 안드로이드 앱의 Mock 데이터를 실제 API 통신(Retrofit 등 활용)으로 교체
    - 오디오 스트리밍 최적화 및 버그 수정
- [ ] **Phase 5: 마무리 및 배포**
    - 최종 UI/UX 폴리싱 및 QA 테스트