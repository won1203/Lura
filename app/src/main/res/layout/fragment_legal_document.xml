<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:fillViewport="true"
    tools:context=".LegalDocumentFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="20dp"
        android:paddingEnd="24dp"
        android:paddingBottom="48dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/legal_document_back_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="-12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/app_info_back_content_description"
                android:padding="12dp"
                android:src="@drawable/ic_chevron_left_24"
                app:tint="@color/lura_text_primary" />

            <TextView
                android:id="@+id/legal_document_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:layout_weight="1"
                android:includeFontPadding="false"
                android:textColor="@color/lura_text_primary"
                android:textSize="26sp"
                android:textStyle="bold"
                tools:text="개인정보 처리방침" />
        </LinearLayout>

        <TextView
            android:id="@+id/legal_document_body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:lineSpacingExtra="5dp"
            android:textColor="@color/lura_text_secondary"
            android:textColorLink="@color/lura_accent"
            android:textIsSelectable="true"
            android:textSize="15sp"
            tools:text="문서 내용" />
    </LinearLayout>
</androidx.core.widget.NestedScrollView>
