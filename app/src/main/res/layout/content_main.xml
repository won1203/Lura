<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <fragment
        android:id="@+id/nav_host_fragment_content_main"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="@dimen/bottom_navigation_height"
        android:background="@color/lura_surface"
        android:minHeight="@dimen/bottom_navigation_height"
        app:itemPaddingBottom="@dimen/bottom_navigation_item_padding_bottom"
        app:itemPaddingTop="@dimen/bottom_navigation_item_padding_top"
        app:itemIconTint="@color/bottom_navigation_item_color"
        app:itemIconSize="@dimen/bottom_navigation_icon_size"
        app:itemRippleColor="@color/lura_chip_background"
        app:itemTextColor="@color/bottom_navigation_item_color"
        app:itemTextAppearanceActive="@style/TextAppearance.Lura.BottomNavigation"
        app:itemTextAppearanceInactive="@style/TextAppearance.Lura.BottomNavigation"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:menu="@menu/bottom_navigation_menu" />
</androidx.constraintlayout.widget.ConstraintLayout>
